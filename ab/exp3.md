# 실험 3 — 저장 1회 비용 · 롤백 안전성

생성: 2026-09-01T19:49:41.288Z

## 3a. 저장 1회의 소스 수집 비용 — 실제 wigss 프로젝트 기준

측정 대상: `/Users/morirokim/wigtn/wigss` · 반복 15회

| 지표 | A (현행 /api/refactor) | B (주소 기반) | 배수 |
|---|---:|---:|---:|
| 읽은 파일 | 40개 | 1개 | 40× |
| 읽은 바이트 | 106,631 | 284 | 375× |
| 수집 지연 | 5.46 ms | 0.074 ms | 74× |

HMR 예산(50~300ms) 대비 A의 수집 단계만으로 3.6% 를 소비한다 (기준 150ms).

v3(P2, PROD-632) 이후 출하 파이프라인은 변경된 컴포넌트 전원이 주소를 가지면 B열로 동작한다 — A열은 주소가 하나라도 없을 때의 저하 경로(D6)로만 남는다.

## 3b. 롤백이 사용자의 동시 수정을 지키는가

시나리오: ① WIGSS 가 className 을 수정 → ② 사용자가 같은 파일의 다른 줄을 수정 → ③ 검증 실패로 롤백

| 결과 | A (파일 전체 복원) | B (범위 역치환) |
|---|---|---|
| 스타일 되돌림 | ✅ | ✅ |
| **사용자 수정 보존** | ❌ 소실 | ✅ 보존 |
| 거부 여부 | 수행 | 수행 |

A 는 사용자 수정을 잃는다. 사라진 줄: `const total = items.filter(Boolean).length;` — 되돌아간 줄: `const total = items.length;`

### 추가 케이스 — 사용자가 우리가 쓴 바로 그 줄을 다시 고쳤을 때

| 결과 | A | B |
|---|---|---|
| 사용자의 h-72 보존 | ❌ 소실 | ✅ |
| 동작 | 조용히 덮어씀 | 거부: 우리가 쓴 내용이 이미 사라졌다 — 롤백 거부 |